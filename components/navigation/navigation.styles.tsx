import { createStyles } from "@mantine/core";

export const useStyles = createStyles((theme) => ({
  container: {
    // position: "fixed",
    // top: "0px",
    // left: 0,
    // width: "100%",
    // zIndex: 50,
    // paddingLeft: 90,
    // paddingRight: 90,
  },
}));
